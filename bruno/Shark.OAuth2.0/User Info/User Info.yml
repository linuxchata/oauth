info:
  name: User Info
  type: http
  seq: 1

http:
  method: GET
  url: "{{HostUri}}/userinfo"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
